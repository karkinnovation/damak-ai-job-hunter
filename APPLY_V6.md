# Apply Awasar v6

Copy the update files over your current `damak-ai-job-hunter` project, then run:

```bat
npm run build
git add .
git commit -m "Fix employer candidate wording and refine homepage CTA"
git pull --rebase origin main
git push origin main
```

Vercel should deploy automatically after the push.
